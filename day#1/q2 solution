#include<bits/stdc++.h>
using namespace std;
int main(){
	string sad,sres, s;
	getline(cin,s);
	 for(int i=0;i<s.size();i++) {
	 	if(s[i]!=' ')
	 		sad.push_back(s[i]);
	 	else{	
	 		sad=' '+sad;
	 		sres.insert(0,sad);
	 		sad.erase();
	 	}
	 }
	sres.insert(0,sad);
	
	 cout<<sres<<endl;
	return 0;
}
